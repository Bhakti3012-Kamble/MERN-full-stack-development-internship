#Git Hands-on
git config --global user.email "you@example.com"
 run only once on system
git config --global user.name "Your Name" 
 run only once on system
git init
 run only once a project
git status
git add .
git commit -m 'Commit Message'
git branch -M main
run only once per project
 git remote add origin https://github.com/Bhakti3012-Kamble/MERN-full-stack-development-internship.gitgit push -u origin main
 changr the url to repo url
git push -u origin main
